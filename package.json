{
  "name": "mobile-check",
  "version": "0.1.0",
  "private": true,
  "description": "MobileCheck — read-only self-assessment for your own phone over USB (Android + iOS), with encrypted local history. Part of C-LAB.",
  "license": "MIT",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
